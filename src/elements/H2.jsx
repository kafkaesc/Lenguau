export default function H1(props) {
	// This is a wrapper component--the element content
	// should be passed via props.
	// eslint-disable-next-line
	return <h1 {...props} className="mb-2 text-2xl text-center"></h1>;
}
